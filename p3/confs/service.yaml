apiVersion: v1
kind: Service
metadata:
  name: wil-service
  namespace: dev
spec:
  type: LoadBalancer
  selector:
    app: wil-app
  ports:
    - protocol: TCP
      port: 8888      # Port exposé par le service
      targetPort: 8888 # Port sur lequel l'app écoute dans le Pod